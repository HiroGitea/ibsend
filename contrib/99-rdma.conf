# RDMA 注册内存需要 pin 住物理页，默认 8MB 的上限会让稍大的池子直接
# ibv_reg_mr 失败（ENOMEM）。
#
# 安装：sudo cp contrib/99-rdma.conf /etc/security/limits.d/
# 然后重新登录，确认 `ulimit -l` 显示 unlimited
*  soft  memlock  unlimited
*  hard  memlock  unlimited
